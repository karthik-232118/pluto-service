const { Router } = require("express");
const { login } = require("../../controller/desktop/auth.controller");
const {} = require("../../validators/public/public.validator");

exports.desktopAuthRoutes = Router().post("/WAQZ45XKvbTZLPX", login);
